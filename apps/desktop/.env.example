# URL the desktop shell loads. Leave unset for local development against a running `pnpm dev:web`
# (defaults to http://localhost:3000). Set to the real deployed web app URL before packaging a
# build for actual distribution — there's no built-in production default, on purpose.
CANTERO_APP_URL="http://localhost:3000"
