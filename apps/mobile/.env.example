# Base URL of the Cantero API, inlined into the bundle at build time.
#
# The simulator can reach the dev machine on localhost, but a physical phone cannot — point this at
# the dev machine's LAN address (e.g. http://192.168.1.20:4000/api) when testing on a real device.
EXPO_PUBLIC_API_URL=http://localhost:4000/api
