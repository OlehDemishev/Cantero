node_modules/
.expo/
dist/
web-build/

# Native projects are generated by `expo prebuild` / `expo run:*` — regenerate rather than commit,
# so app.json stays the single source of truth for native config. Anchored to this folder: the
# ios/ and android/ sources of local modules under modules/ are hand-written and must be kept.
/ios/
/android/

*.log
.env
.env.local
expo-env.d.ts
