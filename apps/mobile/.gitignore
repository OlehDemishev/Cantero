node_modules/
.expo/
dist/
web-build/

# Native projects are generated by `expo prebuild` / `expo run:*` — regenerate rather than commit,
# so app.json stays the single source of truth for native config.
ios/
android/

*.log
.env
.env.local
expo-env.d.ts
